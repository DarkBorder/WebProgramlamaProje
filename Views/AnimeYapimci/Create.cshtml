@model AnimeBox.Models.AnimeYapimci

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>AnimeYapimci</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AnimeId" class="control-label"></label>
                <select asp-for="AnimeId" class ="form-control" asp-items="ViewBag.AnimeId"></select>
            </div>
            <div class="form-group">
                <label asp-for="YapimciId" class="control-label"></label>
                <select asp-for="YapimciId" class ="form-control" asp-items="ViewBag.YapimciId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
